"use client";
import {useCallback,useEffect,useState} from 'react';import {Copy,Link2,LockKeyhole,ShieldCheck,CheckCircle2,Send,ChevronRight,ArrowUpRight,UserRound,CalendarDays,Loader2,RefreshCw,Tag,Clock3,X} from 'lucide-react';
import {Modal,api,useApp,Loading,Empty,Avatar,Badge,Status,Priority,Field,SearchField} from './ui';import {DraftDocument} from './ticket-composer';import {EntityDialog,DataTree} from './momence-tools';import {indiaDate,object,display} from '@/lib/display';import {STAFF,STATUS_LABELS} from '@/lib/constants';import type {TicketRecord,AdvancedDraft} from '@/lib/ticket-contract';
type Resolution={rootCause:string;actionTaken:string;preventiveAction:string;memberOutcome:string;followUpAt?:string};
type Bundle={ticket:TicketRecord;comments:{id:number;authorName:string;authorRole:string;body:string;createdAt:string}[];activities:{id:number;actorName:string;action:string;detail:string;createdAt:string}[];similar:{id:number;ticketNumber:string;title:string;status:string}[];linked:{id:number;ticketNumber:string;title:string;status:string}[];canResolve:boolean;resolution:Resolution|null};
export function TicketDialog({id,open,onClose,onUpdated}:{id:number;open:boolean;onClose:()=>void;onUpdated?:()=>void}){const{notify,user,openAuth}=useApp();const[bundle,setBundle]=useState<Bundle>(),[error,setError]=useState(''),[tab,setTab]=useState('overview'),[busy,setBusy]=useState(false),[note,setNote]=useState(''),[resolution,setResolution]=useState<Resolution>({rootCause:'',actionTaken:'',preventiveAction:'',memberOutcome:''});const[linkOpen,setLinkOpen]=useState(false),[linkQuery,setLinkQuery]=useState(''),[choices,setChoices]=useState<TicketRecord[]>([]),[childId,setChildId]=useState<number>(),[entity,setEntity]=useState<{module:'members'|'sessions';id:string}>(),[localDetails,setLocalDetails]=useState<{title:string;data:unknown}>(),[staff,setStaff]=useState(STAFF);
const load=useCallback(async(reset=false)=>{try{const d=await api<Bundle>('/api/tickets/'+id);setBundle(d);setError('');if(reset)setResolution(d.resolution||{rootCause:'',actionTaken:'',preventiveAction:'',memberOutcome:''});}catch(e){setError((e as Error).message);}},[id]);useEffect(()=>{if(open){setBundle(undefined);setTab('overview');void load(true);void api<{staff:typeof STAFF}>('/api/staff').then(d=>setStaff(d.staff)).catch(()=>{});const timer=setInterval(()=>void load(),15000);return()=>clearInterval(timer);}},[open,load]);useEffect(()=>{if(linkOpen)void api<{tickets:TicketRecord[]}>('/api/tickets').then(d=>setChoices(d.tickets)).catch(e=>notify(e.message,'error'));},[linkOpen,notify]);
async function patch(p:Record<string,unknown>){if(!bundle)return;setBusy(true);try{const d=await api<{followUpTicket?:{id:number;ticketNumber:string}|null}>('/api/tickets/'+id,{method:'PATCH',body:JSON.stringify({...p,version:bundle.ticket.version})});await load();onUpdated?.();window.dispatchEvent(new Event('iris:tickets-updated'));if(d.followUpTicket)notify(`Bike relapse check ${d.followUpTicket.ticketNumber} auto-raised, due in 48 hours.`);else notify('Ticket updated.');}catch(e){notify((e as Error).message,'error');await load();}finally{setBusy(false);}}
async function duplicate(){setBusy(true);try{const d=await api<{ticket:TicketRecord}>('/api/tickets/'+id,{method:'POST',body:JSON.stringify({action:'duplicate'})});notify(`${d.ticket.ticketNumber} created and linked as a duplicate.`);onUpdated?.();setChildId(d.ticket.id);await load();window.dispatchEvent(new Event('iris:tickets-updated'));}catch(e){notify((e as Error).message,'error');}finally{setBusy(false);}}
async function link(otherId:number,remove=false){try{await api('/api/tickets/'+id,{method:'POST',body:JSON.stringify({action:remove?'unlink':'link',relatedId:otherId})});await load();setLinkOpen(false);notify(remove?'Ticket relationship removed.':'Tickets linked.');}catch(e){notify((e as Error).message,'error');}}
async function addNote(){if(!note.trim())return;setBusy(true);try{await api('/api/tickets/'+id+'/comments',{method:'POST',body:JSON.stringify({body:note,isInternal:true})});setNote('');await load();notify('Internal note saved.');}catch(e){notify((e as Error).message,'error');}finally{setBusy(false);}}
async function saveResolution(){setBusy(true);try{await api('/api/tickets/'+id+'/resolution',{method:'PUT',body:JSON.stringify(resolution)});notify('Private resolution saved.');await load();}catch(e){notify((e as Error).message,'error');}finally{setBusy(false);}}
const t=bundle?.ticket;return <><Modal open={open} onClose={onClose} title={t?t.ticketNumber:'Ticket details'} description={t?`${t.category} · Created ${indiaDate(t.createdAt)} · Revision ${t.version}`:'Loading the latest ticket details'} size="wide" footer={<><span className="muted flex-row" style={{fontSize:10}}><ShieldCheck size={13}/>Saved to your workspace · updates every 15s</span><div className="flex-row"><button className="btn" onClick={()=>void load()}><RefreshCw size={13}/>Refresh</button><button className="btn" disabled={busy||!t} onClick={()=>void duplicate()}><Copy size={13}/>Duplicate</button><button className="btn btn-primary" onClick={onClose}>Done</button></div></>}>
{error&&<div className="error-box" style={{marginBottom:16}}>{error}</div>}{!t||!bundle?<Loading/>:<div className="stack"><div className="between wrap"><div className="flex-row wrap"><Status status={t.status}/><Priority priority={t.priority}/><Badge tone="blue">{t.departmentName}</Badge>{!t.resolutionRequired&&<Badge tone="green">Record only</Badge>}</div><button className="text-btn" onClick={()=>{void navigator.clipboard.writeText(window.location.origin+'/tickets/'+id).then(()=>notify('Ticket link copied.')).catch(()=>notify('Your browser blocked clipboard access.','error'));}}><Link2 size={12}/>Copy link</button></div><div className="workspace-tabs" style={{padding:0}}>{['overview','activity','resolution','related'].map(s=><button key={s} className={tab===s?'active':''} onClick={()=>setTab(s)}>{s==='resolution'&&<LockKeyhole size={12}/>} {s[0].toUpperCase()+s.slice(1)}{s==='related'&&<span>{bundle.linked.length+bundle.similar.length}</span>}</button>)}</div>
{tab==='overview'&&<div className="ticket-detail-grid"><div className="stack"><DraftDocument draft={t as AdvancedDraft}/><div className="card card-pad"><div className="between"><h3>Similar tickets</h3><Badge>{bundle.similar.length}</Badge></div><p className="muted" style={{fontSize:11,marginTop:5}}>Same category and subcategory. No AI guesswork.</p>{bundle.similar.map(s=><button className="related-ticket" key={s.id} onClick={()=>setChildId(s.id)}><div><small>{s.ticketNumber}</small><p>{s.title}</p></div><ChevronRight size={13}/></button>)}{!bundle.similar.length&&<p className="secondary" style={{fontSize:12,marginTop:15}}>This is the first ticket of its kind.</p>}</div></div><aside className="detail-sidebar"><div className="detail-section"><h3>Workflow</h3><div className="detail-fields"><Field label="Status"><select disabled={busy} value={t.status} onChange={e=>void patch({status:e.target.value})}>{Object.entries(STATUS_LABELS).map(([k,v])=><option key={k} value={k}>{v}</option>)}</select></Field><Field label="Priority"><select disabled={busy||!t.resolutionRequired} value={t.priority} onChange={e=>void patch({priority:e.target.value})}>{['critical','high','medium','low'].map(k=><option key={k}>{k}</option>)}</select></Field><Field label="Assigned owner"><select disabled={busy} value={t.assignedStaffId??''} onChange={e=>void patch({assignedStaffId:Number(e.target.value)})}>{staff.filter(p=>p.isActive).map(p=><option key={p.id} value={p.id}>{p.name}</option>)}</select></Field><div className="between"><span>Follow-up due</span><span style={{textAlign:'right',fontSize:10}}>{t.slaDueAt?indiaDate(t.slaDueAt):'Not required'}</span></div><button className="btn btn-sm" disabled={busy||t.isEscalated} onClick={()=>void patch({isEscalated:true})}>{t.isEscalated?'Escalated':'Escalate for review'}</button></div></div><div className="detail-section"><h3>Connected context</h3><button className="related-ticket" onClick={()=>t.momenceMemberId&&object(t.momenceContext).source!=='demo'?setEntity({module:'members',id:t.momenceMemberId}):setLocalDetails({title:t.memberName,data:{name:t.memberName,email:t.memberEmail,phone:t.memberPhone,membership:t.membership,note:object(t.momenceContext).source==='demo'?'Demo profile snapshot. This is not a live member record.':'Local ticket contact. No Momence profile is linked.'}})}><div className="flex-row"><Avatar name={t.memberName}/><div><p>{t.memberName}</p><small>{t.momenceMemberId?'Momence member':'Ticket contact'}</small></div></div><ArrowUpRight size={13}/></button>{t.classFormat&&<button className="related-ticket" onClick={()=>t.momenceSessionId&&object(t.customFields.sessionContext).source!=='demo'?setEntity({module:'sessions',id:t.momenceSessionId}):setLocalDetails({title:t.classFormat||'Class',data:{format:t.classFormat,trainer:t.trainer,studio:t.studio,when:t.incidentAt,note:object(t.customFields.sessionContext).source==='demo'?'Demo session snapshot. This is not a live class record.':'Manually recorded class details.'}})}><div><p>{t.classFormat}</p><small>{t.trainer}</small></div><ArrowUpRight size={13}/></button>}</div><div className="info-box"><LockKeyhole size={14}/><span>{bundle.canResolve?'You’re the assigned owner. Your private resolution workspace is available.':'Resolution is private to '+t.assignedStaffName+'.'}</span></div></aside></div>}
{tab==='activity'&&<div className="ticket-detail-grid"><div className="stack"><h3>Care team notes</h3>{bundle.comments.map(n=><div className="note-item" key={n.id}><div className="between"><span>{n.authorName} · {n.authorRole}</span><small>{indiaDate(n.createdAt)}</small></div><p>{n.body}</p></div>)}{!bundle.comments.length&&<p className="secondary" style={{fontSize:12}}>No internal notes yet.</p>}<textarea rows={3} value={note} onChange={e=>setNote(e.target.value)} placeholder="Add context for your team. This does not send a message to the member."/><button className="btn btn-primary" disabled={busy||!note.trim()} onClick={()=>void addNote()}><Send size={13}/>Post internal note</button></div><div><h3 style={{marginBottom:22}}>Timeline</h3>{bundle.activities.map(a=><div className="timeline-row" key={a.id}><p><strong style={{fontWeight:500}}>{a.actorName}</strong> · {a.action.replaceAll('.',' ')}</p><small>{a.detail}</small><br/><small>{indiaDate(a.createdAt)}</small></div>)}</div></div>}
{tab==='resolution'&&(!t.resolutionRequired?<div className="private-lock"><CheckCircle2 size={28}/><h3>No resolution needed</h3><p>This ticket is a record of feedback, appreciation or assessment. No SLA or resolution workflow applies.</p></div>:!bundle.canResolve?<div className="private-lock"><LockKeyhole size={29}/><h3>Reserved for the assigned owner</h3><p>Only {t.assignedStaffName}’s authenticated staff account can access or update resolution details. They are never sent to other users.</p>{!user&&<button className="btn" style={{marginTop:18}} onClick={openAuth}>Sign in</button>}</div>:<div className="stack"><div className="info-box"><ShieldCheck size={15}/>Private resolution workspace · visible only to you as the assigned owner.</div><div className="form-grid">{(['rootCause','actionTaken','preventiveAction','memberOutcome'] as const).map(k=><Field key={k} label={{rootCause:'Root cause',actionTaken:'Action taken *',preventiveAction:'Preventive action',memberOutcome:'Member outcome *'}[k]}><textarea rows={4} value={resolution[k]} onChange={e=>setResolution(r=>({...r,[k]:e.target.value}))}/></Field>)}<Field label="Follow-up date"><input type="datetime-local" value={resolution.followUpAt||''} onChange={e=>setResolution(r=>({...r,followUpAt:e.target.value}))}/></Field></div><div className="flex-row"><button className="btn btn-primary" disabled={busy} onClick={()=>void saveResolution()}>Save private resolution</button><button className="btn" disabled={busy} onClick={()=>void patch({status:'resolved'})}><CheckCircle2 size={13}/>Mark resolved</button></div></div>)}
{tab==='related'&&<div className="stack"><div className="between"><div><h3>Linked tickets</h3><p className="secondary" style={{fontSize:12}}>Manually connect related incidents or follow-ups.</p></div><button className="btn btn-primary" onClick={()=>setLinkOpen(true)}><Link2 size={13}/>Link a ticket</button></div>{bundle.linked.map(s=><div className="related-ticket" key={s.id}><button className="text-btn grow" onClick={()=>setChildId(s.id)} style={{textAlign:'left',display:'block'}}><small className="muted">{s.ticketNumber}</small><p>{s.title}</p></button><Status status={s.status}/><button className="icon-btn" aria-label="Remove relationship" onClick={()=>void link(s.id,true)}><X size={13}/></button></div>)}{!bundle.linked.length&&<Empty title="No relationships yet" detail="Link tickets to connect context without duplicating your work."/>}<h3>Similar category & subcategory</h3>{bundle.similar.map(s=><div className="related-ticket" key={s.id}><button className="text-btn grow" onClick={()=>setChildId(s.id)}>{s.ticketNumber} · {s.title}</button><button className="btn btn-sm" onClick={()=>void link(s.id)}><Link2 size={12}/>Link</button></div>)}</div>}
</div>}</Modal><Modal open={linkOpen} onClose={()=>setLinkOpen(false)} title="Connect the dots" description="Search and link another ticket." size="narrow"><div className="stack"><SearchField value={linkQuery} onChange={setLinkQuery} placeholder="Search ticket number or title…"/>{choices.filter(s=>s.id!==id&&(s.title+' '+s.ticketNumber).toLowerCase().includes(linkQuery.toLowerCase())&&!bundle?.linked.some(l=>l.id===s.id)).slice(0,20).map(s=><button className="related-ticket" key={s.id} onClick={()=>void link(s.id)}><div><small>{s.ticketNumber}</small><p>{s.title}</p></div><Link2 size={14}/></button>)}</div></Modal>{childId&&<TicketDialog id={childId} open onClose={()=>setChildId(undefined)} onUpdated={()=>void load()}/>} {entity&&<EntityDialog open onClose={()=>setEntity(undefined)} module={entity.module} id={entity.id}/>} {localDetails&&<Modal open onClose={()=>setLocalDetails(undefined)} title={localDetails.title} description="Context recorded on this ticket"><DataTree data={localDetails.data}/></Modal>}</>;}
